# Prometheus 抓取配置
# 定期从 OTel Collector 的 Prometheus exporter 端点拉取指标数据

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: [ 'otel-collector:8889' ]
    # 抓取间隔，平衡数据粒度与存储开销
    scrape_interval: 20s
